// ProductDetailModal removed from admin-site/components (public-only).
// Provide a minimal stub (named + default export) to avoid accidental imports
// and to keep TypeScript checks clean for the admin site.

export function ProductDetailModal(): null {
  return null
}

export default ProductDetailModal
