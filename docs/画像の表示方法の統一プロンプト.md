サーバから受け取った image フィールド（main / attachments / attachment_image_keys）を読み取る際に site-settings と同じ normalize（URL → key の抽出、/cdn-cgi/image/... プレフィックス削除）処理を行う。
正規化した key を使って public URL を必ず生成して preview state にセットする：
preview = getPublicImageUrl( db.images.getUpload(normalizedKey) || normalizedKey ) || ''