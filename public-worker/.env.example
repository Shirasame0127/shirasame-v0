# public-worker example environment variables
# Copy to your Worker environment configuration (wrangler.toml or env vars) and set real values.

# Prefer a Cloudflare-proxied custom domain for images (images.shirasame.com)
IMAGES_DOMAIN=https://images.shirasame.com

# Fallback: R2 public host (only used if IMAGES_DOMAIN is not set)
R2_PUBLIC_URL=https://images.shirasame.com

# R2 bucket name used in worker
R2_BUCKET=images

# Supabase config for reading/writing metadata
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional: internal API base used by worker for proxying
INTERNAL_API_BASE=
INTERNAL_API_KEY=
